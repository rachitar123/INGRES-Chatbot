# Copy this file to .env (never commit .env)
# On Render Static Site, set this as an environment variable in the Render dashboard.

# Required: URL of the deployed FastAPI backend service
REACT_APP_API_URL=https://ingres-backend.onrender.com
